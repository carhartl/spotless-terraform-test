plugins {
  id "com.diffplug.spotless" version "5.15.0"
}

spotless {
  format 'terraform', {
    target 'terraform/**/*.tf'
    nativeCmd 'terraform', '/opt/homebrew/bin/terraform', ['fmt', '-']
  }
}
